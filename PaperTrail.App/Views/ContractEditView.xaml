<UserControl x:Class="PaperTrail.App.Views.ContractEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:PaperTrail.Core.Models;assembly=PaperTrail.Core"
             x:Name="Root"
             AllowDrop="True">

    <!-- Local fallback styles/resources -->
    <UserControl.Resources>
        <!-- Simple tag chip style -->
        <Style x:Key="TagChipStyle" TargetType="Border">
            <Setter Property="Padding" Value="6,2"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Background" Value="#EEF2FF"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Command Bar -->
        <DockPanel Margin="6" LastChildFill="False">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <Button Content="Save"   Command="{Binding SaveCommand}"   Margin="0,0,6,0"/>
                <Button Content="Cancel" Command="{Binding CancelCommand}" Margin="0,0,6,0"/>
                <Button Content="Delete" Command="{Binding DeleteCommand}"/>
            </StackPanel>

            <!-- Status selector -->
            <ComboBox Width="160"
                      DockPanel.Dock="Right"
                      ItemsSource="{Binding ContractStatusValues}"
                      SelectedItem="{Binding SelectedStatus}"/>
        </DockPanel>

        <!-- Main Tabs -->
        <TabControl Grid.Row="1">
            <!-- Details Tab -->
            <TabItem Header="Details">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="12">
                        <TextBlock Text="Client"/>
                        <TextBox Text="{Binding PartySearchText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,4"/>
                        <ComboBox ItemsSource="{Binding Parties}"
                                  SelectedItem="{Binding SelectedParty}"
                                  DisplayMemberPath="Name" />

                        <Button Content="Add Client"
                                Command="{Binding AddPartyCommand}"
                                Width="120"
                                Margin="0,6,0,0" />

                        <TextBlock Text="Title" Margin="0,12,0,0"/>
                        <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBlock Text="Value" Margin="0,12,0,0"/>
                        <TextBox Text="{Binding ValueAmount, StringFormat=C}"/>

                        <TextBlock Text="Tags" Margin="0,12,0,0"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBox Width="180" Text="{Binding NewTagText}"/>
                            <Button Content="Add"
                                    Command="{Binding AddTagCommand}"
                                    Margin="6,0,0,0"/>
                        </StackPanel>

                        <ItemsControl ItemsSource="{Binding Tags}" Margin="0,6,0,0">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource TagChipStyle}">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding}" Margin="0,0,6,0"/>
                                            <!-- Use ElementName=Root to reach the VM from inside template -->
                                            <Button Content="✕"
                                                    Padding="2,0"
                                                    Command="{Binding DataContext.RemoveTagCommand, ElementName=Root}"
                                                    CommandParameter="{Binding}"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Dates Tab -->
            <TabItem Header="Dates">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <Grid Margin="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Effective" Grid.Row="0" VerticalAlignment="Center"/>
                        <DatePicker SelectedDate="{Binding EffectiveDateDateTime}"
                                    Grid.Row="0" Grid.Column="1"/>

                        <TextBlock Text="Renewal" Grid.Row="1" VerticalAlignment="Center"/>
                        <DatePicker SelectedDate="{Binding RenewalDateDateTime}"
                                    Grid.Row="1" Grid.Column="1"/>

                        <TextBlock Text="Renewal Term (months)" Grid.Row="2" VerticalAlignment="Center"/>
                        <TextBox Text="{Binding RenewalTermMonths}" Grid.Row="2" Grid.Column="1"/>

                        <TextBlock Text="Notice Period (days)" Grid.Row="3" VerticalAlignment="Center"/>
                        <TextBox Text="{Binding NoticePeriodDays}" Grid.Row="3" Grid.Column="1"/>

                        <TextBlock Text="Termination" Grid.Row="4" VerticalAlignment="Center"/>
                        <DatePicker SelectedDate="{Binding TerminationDateDateTime}"
                                    Grid.Row="4" Grid.Column="1"/>

                        <TextBlock Text="Next Renewal" Grid.Row="5" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding ComputedNextRenewalText}" Grid.Row="5" Grid.Column="1"/>

                        <TextBlock Text="Notice Date" Grid.Row="6" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding ComputedNoticeDateText}" Grid.Row="6" Grid.Column="1"/>
                    </Grid>
                </ScrollViewer>
            </TabItem>

            <!-- Attachments Tab -->
            <TabItem Header="Attachments">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="6" Padding="10" Margin="0,0,0,10"
                            Background="#F8FAFC">
                        <TextBlock Text="Drag files here to attach" HorizontalAlignment="Center"/>
                    </Border>

                    <ListView Grid.Row="1" ItemsSource="{Binding Attachments}">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="File" DisplayMemberBinding="{Binding FileName}" Width="280"/>
                                <GridViewColumn Header="Hash" DisplayMemberBinding="{Binding Hash}" Width="260"/>
                                <GridViewColumn Header="Actions" Width="200">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <Button Content="Open"
                                                        Command="{Binding DataContext.OpenAttachmentCommand, ElementName=Root}"
                                                        CommandParameter="{Binding}" Margin="0,0,6,0"/>
                                                <Button Content="Reveal"
                                                        Command="{Binding DataContext.RevealAttachmentCommand, ElementName=Root}"
                                                        CommandParameter="{Binding}" Margin="0,0,6,0"/>
                                                <Button Content="Remove"
                                                        Command="{Binding DataContext.RemoveAttachmentCommand, ElementName=Root}"
                                                        CommandParameter="{Binding}"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>

                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button Content="Add Files..." Command="{Binding AddAttachmentCommand}"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <!-- Reminders Tab -->
            <TabItem Header="Reminders">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <DataGrid ItemsSource="{Binding Reminders}" AutoGenerateColumns="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="*" />
                            <DataGridTextColumn Header="Due (UTC)" Binding="{Binding DueUtc}" Width="2*" />
                            <DataGridTextColumn Header="Completed (UTC)" Binding="{Binding CompletedUtc}" Width="2*" />
                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                        <Button Content="Add Reminder" Command="{Binding AddReminderCommand}" Width="140"/>
                    </StackPanel>
                </Grid>
            </TabItem>

            <!-- Notes Tab -->
            <TabItem Header="Notes">
                <TextBox Margin="12"
                         Text="{Binding Notes}"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"/>
            </TabItem>
        </TabControl>

        <!-- Error summary (optional) -->
        <ItemsControl Grid.Row="2" ItemsSource="{Binding ErrorList}" Margin="6"/>
    </Grid>
</UserControl>
