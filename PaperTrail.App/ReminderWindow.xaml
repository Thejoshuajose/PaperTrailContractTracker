<Window x:Class="PaperTrail.App.ReminderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Reminder" Height="200" Width="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Date:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <DatePicker x:Name="datePicker"/>
        </StackPanel>
        <TextBlock Grid.Row="1" Text="Notes:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="2" x:Name="noteText" Height="60" AcceptsReturn="True"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <CheckBox x:Name="calendarCheck" Content="Add to calendar" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <Button Content="OK" Width="75" Margin="0,0,5,0" IsDefault="True" Click="Ok_Click"/>
            <Button Content="Cancel" Width="75" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
